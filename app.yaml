runtime: python311
entrypoint: gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app

env_variables:
  DB_USER: "postgres"
  DB_PASSWORD: "TU_PASSWORD_REAL"
  DB_NAME: "students_db"
  INSTANCE_CONNECTION_NAME: "prueba-ufps-489021:us-central1:fastapi-db"
